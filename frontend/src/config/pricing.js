/**
 * Initial-stage pricing (e.g. local NYC).
 * Local airport transfer: flat rate.
 * Hourly: rate per hour; total = rate × hours.
 */
export const PRICING = {
  /** Local (NYC) airport transfer - one way flat rate (USD) */
  LOCAL_AIRPORT_TRANSFER: 197,
  /** Hourly hire rate per hour (USD) */
  HOURLY_RATE_PER_HOUR: 97,
};

export default PRICING;
